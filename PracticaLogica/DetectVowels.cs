using System;
/* public static class Kata
{
    public static string Disemvowel(string str)
    {
        /* List<string> TargetVowelsLower = new List<string>{"a","e","i","o","u"};
        List<string> TargertVowelsCapital = new List<string>();
        List<string> Target= new List<string>();
        string  nuevaPalabra = "";

            foreach(string letra in TargetVowelsLower)
            {
                TargertVowelsCapital.Add(letra.ToUpper());

            }
           
            TargertVowelsCapital.AddRange(TargetVowelsLower);
            Target.AddRange(TargertVowelsCapital);
             Target.ForEach(x => Console.WriteLine(x));
            Console.WriteLine(str);
        foreach(char   comparador in str)
        {       
            
            
            if(!(Target.Contains(comparador.ToString())))
            {
                nuevaPalabra += comparador;
            }
        }

        return  nuevaPalabra;

  
  {       public static string Disemvowel(string str)*/
   /*  return string.Concat(str.Where(ch => !"aeiouAEIOU".Contains(ch)));
  
 
    } 
} */